package com.example.myapplication;import android.app.Activity;import android.content.Intent;import android.graphics.drawable.Drawable;import android.os.Bundle;import androidx.annotation.Nullable;import android.view.View;import android.widget.Button;import android.widget.ImageButton;public class LoginActivity extends Activity {    int i = 0;    boolean flag;    @Override    protected void onCreate(@Nullable Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_login);        final ImageButton ibtn = findViewById(R.id.img_btn);        ibtn.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                if (flag){                    ibtn.setBackgroundResource(R.drawable.one);                    flag = false;                }else {                    ibtn.setBackgroundResource(R.drawable.two);                    flag = true;                }            }        });        final Button btn = findViewById(R.id.img_btn1);        btn.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                Drawable wallet1 = getDrawable(R.drawable.wallet1);                Drawable wallet2 = getDrawable(R.drawable.wallet2);                switch (i){                    case 0: btn.setCompoundDrawablesWithIntrinsicBounds(wallet1,null,null,null);i++;break;                    case 1: btn.setCompoundDrawablesWithIntrinsicBounds(wallet2,null,null,null);i++;break;                }                if (i >= 2)                    i = 0;            }        });        Button bt_start = findViewById(R.id.btn_next_2);        bt_start.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                Intent intent = new Intent();                intent.setAction("thr_avtivity");                startActivity(intent);            }        });    }}